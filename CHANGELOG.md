# Changelog

## 1.1.7

- 新增基础DC硬性量表与范围：优先使用 5/8/10/12/15/18/20/22/25/30 难度锚点。
- 正式 D20_REQUEST 的 DC 现在只允许 5—30；一般应为 5—25，DC 30 仅用于仍然客观可行但近乎极限的特殊行动。
- 当合理难度需要超过 30 时，不再用 35/50/75/100 等异常数字硬塞进 D20，而是判定当前条件下无法完成，或要求先改变客观条件后再尝试。
- 明确区分 DC 与 MOD：任务固有难度进入 DC；角色、功法、装备、状态、环境、敌方针对性能力进入 MOD/DETAIL；禁止同一事实重复计算。
- 请求解析器新增 DC 范围硬校验。模型即使输出 DC=75，也会被拒绝投骰并提示重新评估。
- 新增回归测试，覆盖 DC=30 合法以及 DC=4/31/75/100 被拒绝。
- 版本升级至 1.1.7。

## 1.1.6

- 新增天道点自动获取机制，固定上限 5 点。
- USER 正式完成一次新的境界/小境界突破：自动 +1 天道点。
- USER 完成一个新的秘境：自动 +1 天道点。
- USER 首次征服一名新的女修：自动 +1 天道点；同一名女修只奖励一次。
- 新增隐藏协议 `[D20_TIANDAO_AWARD]`，由模型只在里程碑已经正式成为世界事实后输出；扩展负责实际加点、去重与隐藏协议清理。
- 突破/秘境/女修分别使用稳定 KEY 去重；重复叙述、关系加深、再次进入同一秘境或重复同一次突破不会再次奖励。
- 天道点达到 5/5 时，新的里程碑仍记录为已处理，避免之后通过重复生成刷取。
- D20 待结算期间禁止提前发放里程碑奖励；只有正式接受并结算后才允许触发。
- 新增 `/xiuxian-d20-tiandao-reset` 内部重置命令与自动奖励回归测试。
- 所有改动仍只发生在 D20 扩展内部，不读取后写回、复制或修改用户第三方预设。

## 1.1.5

- 修复修正提取链路：自动模式直接读取最新 AI 原始消息中的单个完整 `D20_REQUEST`，忽略思考块内的草稿，不再从整条消息分别取第一个 `MOD/DETAIL`。
- 投骰前校验 ACTION/DC/MOD/DETAIL、整数格式、重复字段/因素与明细合计；出错不投骰、不补零、不覆盖上一组缓存。
- 保留正负相抵得到的 MOD=0；全部零值占位须先说明真实原因，扩展不会编造加减项。
- 从旧版核心规则恢复丢失的修正明细格式、计算示例与零项处理约定，全部变更限于扩展资源。
- 修复 v1.1.3 留言清理跨越多个折叠块、误删已有状态与 D20 请求的问题；保护请求原文并同步当前 swipe。
- 修复 v1.1.0 起预写内部版本导致升级跳过 QR/Regex 覆盖的问题；统一安装器、入口与 manifest 版本，配置保存成功后才记录新版本。
- 识别原始导入集合 `修仙D20 ` 的尾随空格：更新其中 D20 按钮，关闭其重复自动处理器，保留自定义按钮。
- `测试DETAIL` 提供原始请求、合计、缓存和错误原因诊断，已锁定骰点及天道点不因升级改变。
- 手动兼容 Regex 同步限定请求边界；独立手动 QR 保持可用，其校验能力与扩展模式的差异见手动说明。
- 新增 16 项自动测试；回归证据、提交定位与未验证范围见 `docs/modifier-regression.md`。

## 1.1.4

- 增加包装层版本显示修正。该修正仍保留升级前预写基础版本的逻辑，v1.1.5 已移除这一问题。

## 1.1.3

- 新增扩展层 D20 等待交互硬拦截，不修改任何用户预设。
- 当一条 AI 消息包含 `[D20_REQUEST]` 时，自动从该条消息中移除 `<options>...</options>` 行动选项块。
- 同时移除 `<details><summary>小猫之神的留言</summary>...</details>` 留言块。
- 兼容流式生成：消息入列时先清理，生成结束/停止后再做一次校验并重绘。
- 接受 D20 结果后的正式结算消息不会被该规则误删；只有包含新的 `[D20_REQUEST]` 的消息才触发拦截。
- 明确记录 `presetMutation=false`：扩展不会读取后写回、复制、重命名或修改用户现有预设。

## 1.1.2

- 新增 D20 交互冻结规则。
- 一旦输出 `[D20_REQUEST]`，该请求必须成为本轮最后一个有效输出块。
- 从请求发出到 `[D20_ACCEPT]` 前，禁止生成候选回复、推荐回复、“请选择回应”、三个行动选项、下一步建议等替代 USER 操作的内容。
- 待结算期间仅允许外部 D20 系统提供“接受当前结果 / 重Roll”等操作。
- 收到 `[D20_ACCEPT]` 并完成正式结算后，原有候选回复/推荐行动模块恢复正常。
- 将规则中的特定第三方预设称呼改为通用“当前预设 / 状态系统兼容”表述。

## 1.1.1

- 不再复制、重命名或生成任何第三方预设。
- 固定只提供并自动注入两套独立 D20 规则模板：`修仙D20·核心裁判系统`、`修仙D20·每轮强制检查`。
- 移除“原预设名 + 修仙D20”模式及相关状态记录。

## 1.0.0

- 首次公开打包版。
- 自动安装 4 条 D20 Regex。
- 自动创建/修复 `修仙D20` Quick Reply Set（7 个内置 QR）。
- 自动挂载为全局 Quick Reply Set。
- 首次安装/手动修复时开启 Quick Replies。
- 增加状态检查、修复按钮与防重复逻辑。
- 保留非本扩展管理的 Regex 与额外 QR。
- 修正 `D20_提取ACTION` 的 `replaceString` 为 `$1`。
- 去除 Quick Reply Set 名称末尾空格。