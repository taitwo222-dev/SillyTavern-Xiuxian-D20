# Changelog

## 1.0.0

- 首次公开打包版。
- 自动安装 4 条 D20 Regex。
- 自动创建/修复 `修仙D20` Quick Reply Set（7 个内置 QR）。
- 自动挂载为全局 Quick Reply Set。
- 首次安装/手动修复时开启 Quick Replies。
- 增加状态检查、修复按钮与防重复逻辑。
- 保留非本扩展管理的 Regex 与额外 QR。
- 修正 `D20_提取ACTION` 的 `replaceString` 为 `$1`。
- 去除 Quick Reply Set 名称末尾空格。
